output:
  - failure

pre-commit:
  jobs:
    - name: fmt
      glob: "*.{js,ts,json,astro}"
      run: pnpm run fmt -- {staged_files}
      stage_fixed: true
    - name: lint
      glob: "*.{js,ts,astro}"
      run: pnpm run lint:fix -- {staged_files}
      stage_fixed: true
